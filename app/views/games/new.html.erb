<div class="m-3">
  <h1> New Game </h1>
  <p><%= @letters %></p>
  <%# <div class="card text-dark bg-info mb-3" style="max-width: 3rem;">
    <%= @letters.each do |letter| %>
      <%# <h5 class="card-title" style="border: 1px black; padding: 1.25rem;"><%= letter %></h5>
    <%# <% end %>
  <%# </div>  %>
  <form action="/score" method="post" data-turbo="false">
    <label class="form-label">What is the longest word you can find?</label>
      <div class="input-group mb-3">
        <input type="text" class="form-control" name="answer" placeholder="Type your answer here">
        <div class="input-group-append">
          <button type="submit" class="btn btn-primary">Play</button>
        </div>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  </form>
</div>
